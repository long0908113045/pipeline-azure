apiVersion: v1
kind: Service
metadata:
    name: examdevopscontainerregistry
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: examdevopscontainerregistry
